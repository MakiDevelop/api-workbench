{
  "name": "api-workbench-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "desktop:dev": "vite --config vite.config.mjs --host 127.0.0.1 --port 1420",
    "desktop:build": "vite build --config vite.config.mjs",
    "desktop:prepare-sidecar": "node scripts/prepare-sidecar.mjs",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:mac": "tauri build --bundles app"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^6.0.0"
  }
}
